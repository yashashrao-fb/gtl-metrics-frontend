// Stub for @libs/shared/types — only used by auth-export's featureGuard
// which is not actively used in this app.
export type FeatureFlag = string;
export const GloballyReleasedFeatures: readonly string[] = [];
